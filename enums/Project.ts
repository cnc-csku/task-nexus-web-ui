export enum ProjectStatus {
  Active = "ACTIVE",
  Archived = "ARCHIVED",
}

export enum AttributeType {
  String = "STRING",
  Number = "NUMBER",
  Boolean = "BOOLEAN",
  Date = "DATE",
}

export enum ProjectMemberRole {
  Admin = "ADMIN",
  Moderator = "MODERATOR",
  Member = "MEMBER",
}
